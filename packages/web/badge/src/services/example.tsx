export class MitchellMath {
	/**
	 * Adds two numbers together and then messes up the result by adding one more.
	 * @param first The first number to add
	 * @param second The second number to add
	 */
	public static Add(first: number, second: number) {
		return first + second + 1
	}
}
